# پروژه چشمان - دستور العمل توسعه و استقرار

## 📋 فهرست

1. [شروع سریع](#شروع-سریع)
2. [ساختار پروژه](#ساختار-پروژه)
3. [نصب و راه‌اندازی](#نصب-و-راهاندازی)
4. [توسعه](#توسعه)
5. [استقرار](#استقرار)
6. [نمایش تصاویر](#نمایش-تصاویر)

---

## 🚀 شروع سریع

### پیش‌نیازها
- Node.js 16 یا بالاتر
- npm یا yarn
- Git (اختیاری)

### مراحل شروع

```bash
# 1. رفتن به پوشه پروژه
cd c:\Users\Administrator\Documents\projects\chechman

# 2. نصب dependencies
npm install

# 3. اجرای سرور توسعه
npm run dev

# 4. باز کردن در مرورگر
# سایت در آدرس http://localhost:5173 باز می‌شود
```

---

## 📁 ساختار پروژه

```
chechman/
├── public/
│   ├── manifest.json          # PWA Manifest
│   ├── service-worker.js      # Service Worker
│   ├── icon-192.png          # App Icon
│   └── icon-512.png          # App Icon
│
├── src/
│   ├── css/
│   │   └── style.css         # استایل‌های اصلی و Tailwind
│   │
│   ├── js/
│   │   ├── main.js           # نقطه ورود اپلیکیشن
│   │   ├── router.js         # سیستم مسیریابی SPA
│   │   ├── config.js         # تنظیمات پروژه
│   │   ├── store.js          # مدیریت state
│   │   ├── components.js     # کامپوننت‌های مشترک
│   │   ├── utils.js          # توابع کمکی
│   │   │
│   │   └── pages/
│   │       ├── landing.js    # صفحه خانگی
│   │       ├── store.js      # صفحه فروشگاه
│   │       ├── cart.js       # صفحه سبد خرید
│   │       └── admin.js      # صفحه پنل مدیریت
│   │
│   └── assets/               # تصاویر و منابع
│
├── index.html                # صفحه HTML اصلی
├── vite.config.js           # تنظیمات Vite
├── tailwind.config.js       # تنظیمات Tailwind CSS
├── postcss.config.js        # تنظیمات PostCSS
├── tsconfig.json            # تنظیمات TypeScript
├── package.json             # مشخصات و dependencies
├── README.md                # توضیحات اصلی
├── SETUP_GUIDE.md           # راهنمای نصب
└── DEVELOPMENT.md           # این فایل

```

---

## 🔧 نصب و راه‌اندازی

### مرحله 1: نصب Node.js

دانلود از: https://nodejs.org/

نسخه **LTS** را انتخاب کنید.

### مرحله 2: نصب پروژه

```bash
# رفتن به پوشه پروژه
cd c:\Users\Administrator\Documents\projects\chechman

# نصب تمام dependencies
npm install

# (یا استفاده از yarn)
yarn install
```

### مرحله 3: اجرای Development Server

```bash
npm run dev
```

سایت در آدرس `http://localhost:5173` باز می‌شود.

### مرحله 4: Build برای Production

```bash
npm run build
```

فایل‌های build شده در پوشه `dist` قرار می‌گیرند.

### مرحله 5: Preview Build

```bash
npm run preview
```

---

## 💻 توسعه

### کامپوننت‌های اصلی

#### 1. **Landing Page** (`src/js/pages/landing.js`)
- Hero Section
- Eye Emoji Tracker (Mouse Following)
- دو دکمه ورود اصلی
- Bottom Navigation

#### 2. **Store Page** (`src/js/pages/store.js`)
- Grid محصولات (2 ستون)
- سیستم فیلتر
- Like Button
- Add to Cart

#### 3. **Cart Page** (`src/js/pages/cart.js`)
- نمایش محصولات سبد
- کنترل تعداد (+/-)
- حذف محصول
- محاسبه جمع کل
- دکمه تکمیل خرید

#### 4. **Admin Page** (`src/js/pages/admin.js`)
- Dashboard با 4 کارت وضعیت
- مدیریت محصولات
- مدیریت انبار
- حسابداری
- مدیریت سفارشات

### State Management

فایل `store.js` شامل:
- `getState()` - دریافت state
- `addToCart()` - افزودن به سبد
- `removeFromCart()` - حذف از سبد
- `toggleLike()` - Like کردن
- `filterProducts()` - فیلتر محصولات

### Routing System

فایل `router.js` شامل:
- `initApp()` - مقدار دهی اپلیکیشن
- `renderPage()` - رندر صفحات
- `getCurrentPage()` - صفحه فعلی

### Styling

#### Colors
```
🎨 Pink Scream: #FF10F0
🎨 Yellow Canary: #FFD700
🎨 Green Lawn: #32CD32
```

#### Classes
```css
.btn-neo          /* دکمه Neo-Brutalism */
.btn-neo-pink     /* دکمه صورتی */
.btn-neo-yellow   /* دکمه زرد */
.btn-neo-green    /* دکمه سبز */
.card-neo         /* کارت Neo-Brutalism */
.title-neo        /* تیتر Neo-Brutalism */
```

---

## 📦 استقرار

### 1. استقرار بر روی GitHub Pages

```bash
# Build پروژه
npm run build

# Push به GitHub
git add .
git commit -m "Deploy"
git push origin main
```

### 2. استقرار بر روی Vercel

```bash
# نصب Vercel CLI
npm install -g vercel

# Deploy
vercel
```

### 3. استقرار بر روی Netlify

```bash
# نصب Netlify CLI
npm install -g netlify-cli

# Deploy
netlify deploy --prod --dir=dist
```

### 4. استقرار بر روی سرور شخصی

```bash
# Build پروژه
npm run build

# Upload فایل‌های dist به سرور
# سپس serve کردن آن‌ها

# مثال با Node.js:
npm install -g serve
serve -s dist
```

---

## 🎨 نمایش تصاویر

### Landing Page
```
┌─────────────────────────────────────┐
│                                     │
│         👀 چشمان 👀                 │
│   فروشگاه تخصصی عینک شما           │
│                                     │
│  [ورود به فروشگاه]  [پنل مدیریت]   │
│                                     │
└─────────────────────────────────────┘
```

### Store Page
```
┌─────────────────────────────────────┐
│  فروشگاه عینک                  🛒 6 │
├─────────────────────────────────────┤
│  [آفتابی] [طبی] [ورزشی] [خلبانی]   │
├─────────────────────────────────────┤
│  ┌────────┐  ┌────────┐              │
│  │ عینک 1 │  │ عینک 2 │              │
│  │ 450000 │  │ 350000 │              │
│  │ 🤍     │  │ ❤️     │              │
│  │ [افزود]│  │ [افزود]│              │
│  └────────┘  └────────┘              │
└─────────────────────────────────────┘
```

### Cart Page
```
┌─────────────────────────────────────┐
│  سبد خریدتان                        │
├─────────────────────────────────────┤
│  عینک آفتابی                       │
│  450000 تومان                       │
│  [+] 1 [-]        [حذف]            │
├─────────────────────────────────────┤
│  ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓  │
│  ┃ جمع کل: 2,550,000 تومان     ┃  │
│  ┃                              ┃  │
│  ┃  [تکمیل خریدی]              ┃  │
│  ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛  │
└─────────────────────────────────────┘
```

### Admin Panel
```
┌──────────────┬─────────────────────┐
│              │                     │
│   Dashboard  │  ┌────┬────┬────┬───┐
│              │  │💰18│👥128│📦24│⏳23
│  مدیریت...   │  └────┴────┴────┴───┘
│              │                     │
│  مدیریت...   │  نمودار فروش هفتگی  │
│              │                     │
│  مدیریت...   │                     │
└──────────────┴─────────────────────┘
```

---

## 🔐 امنیت

### بهترین روش‌ها

1. **Validate Input**: همیشه input‌های کاربر را validate کنید
2. **XSS Prevention**: از `.textContent` استفاده کنید نه `.innerHTML`
3. **CSRF Protection**: از CSRF tokens استفاده کنید
4. **SSL/TLS**: همیشه HTTPS استفاده کنید

---

## 📱 PWA Features

### Manifest
فایل `public/manifest.json` شامل:
- App Name و Icon
- Theme Color
- Display Mode
- Start URL

### Service Worker
فایل `public/service-worker.js` برای:
- Offline Support (اختیاری)
- Cache Management
- Push Notifications (اختیاری)

---

## 🧪 تست

### Manual Testing
- بررسی تمام صفحات
- تست دکمه‌ها و لینک‌ها
- تست فیلتر و جستجو
- تست سبد خرید

### Browser Testing
- Chrome/Edge
- Firefox
- Safari
- Mobile Browsers

---

## 🐛 رفع اشکالات

### مشکل: npm install نمی‌کند
```bash
# حل:
rm -r node_modules package-lock.json
npm cache clean --force
npm install
```

### مشکل: Vite port استفاده شده است
```bash
# حل:
npm run dev -- --port 5174
```

### مشکل: تغییرات رندر نمی‌شوند
```bash
# حل:
# Hard Refresh: Ctrl+Shift+R (Windows/Linux)
# Hard Refresh: Cmd+Shift+R (Mac)
```

---

## 📚 منابع

- [Vite Documentation](https://vitejs.dev)
- [Tailwind CSS](https://tailwindcss.com)
- [MDN Web Docs](https://developer.mozilla.org)

---

## 📞 تماس و پشتیبانی

برای سوالات و پیشنهادات:
- **ایمیل**: support@cheshman.com
- **تلفن**: 021-XXXX-XXXX

---

**آخرین بروزرسانی**: 1402/10/17
**نسخه**: 1.0.0

---

# Happy Coding! 🚀👀
